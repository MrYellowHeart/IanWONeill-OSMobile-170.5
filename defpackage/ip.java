package p000;

public class ip {
    static final int aa = 100;
    public static final int as = 536870912;
    static final int bj = 7;
    int ad;
    String[] af;

    public ip(String[] strArr) {
        try {
            this.ad = 0;
            this.af = strArr;
        } catch (Throwable e) {
            throw ba.ad(e, "ip.<init>(" + ')');
        }
    }

    public String aa() {
        while (1172795223 * this.ad < this.af.length) {
            String[] strArr = this.af;
            int i = this.ad - 90021309;
            this.ad = i;
            String trim = strArr[(i * -1630705961) - 1].trim();
            if (trim.startsWith("[") && trim.endsWith("]")) {
                return trim.substring(1, trim.length() - 1).trim();
            }
        }
        return null;
    }

    public String af(byte b) {
        while (this.ad * 1529838187 < this.af.length) {
            try {
                String[] strArr = this.af;
                int i = this.ad - 90021309;
                this.ad = i;
                String trim = strArr[(i * 1529838187) - 1].trim();
                if (trim.startsWith("[") && trim.endsWith("]")) {
                    return trim.substring(1, trim.length() - 1).trim();
                }
            } catch (Throwable e) {
                throw ba.ad(e, "ip.af(" + ')');
            }
        }
        return null;
    }

    public String ag() {
        while (this.ad * 1529838187 < this.af.length) {
            String[] strArr = this.af;
            int i = this.ad - 90021309;
            this.ad = i;
            String trim = strArr[(i * 1529838187) - 1].trim();
            if (trim.startsWith("[") && trim.endsWith("]")) {
                return trim.substring(1, trim.length() - 1).trim();
            }
        }
        return null;
    }

    public String an() {
        while (this.ad * 1529838187 < this.af.length) {
            String[] strArr = this.af;
            int i = this.ad - 90021309;
            this.ad = i;
            String trim = strArr[(i * 1529838187) - 1].trim();
            if (trim.startsWith("[") && trim.endsWith("]")) {
                return trim.substring(1, trim.length() - 1).trim();
            }
        }
        return null;
    }

    public String[] ad(int i) {
        String[] strArr = null;
        while (this.ad * 1529838187 < this.af.length) {
            try {
                String[] strArr2 = this.af;
                int i2 = this.ad - 90021309;
                this.ad = i2;
                String trim = strArr2[(i2 * 1529838187) - 1].trim();
                if (!(trim.startsWith("#") || trim.startsWith("//") || trim.length() == 0)) {
                    if (trim.startsWith("[") && trim.endsWith("]")) {
                        this.ad -= -90021309;
                        break;
                    }
                    if (-1 != trim.indexOf(61)) {
                        strArr = new String[]{trim.substring(0, trim.indexOf(61)).trim(), trim.substring(trim.indexOf(61) + 1).trim()};
                        break;
                    }
                }
            } catch (Throwable e) {
                throw ba.ad(e, "ip.ad(" + ')');
            }
        }
        return strArr;
    }

    public String[] as() {
        while (this.ad * 1529838187 < this.af.length) {
            String[] strArr = this.af;
            int i = this.ad - 90021309;
            this.ad = i;
            String trim = strArr[(i * 1529838187) - 1].trim();
            if (!(trim.startsWith("#") || trim.startsWith("//") || trim.length() == 0)) {
                if (trim.startsWith("[") && trim.endsWith("]")) {
                    this.ad -= -1799248218;
                    return null;
                }
                if (-1 != trim.indexOf(284180223)) {
                    return new String[]{trim.substring(0, trim.indexOf(284180223)).trim(), trim.substring(trim.indexOf(284180223) + 1).trim()};
                }
            }
        }
        return null;
    }

    public String[] ay() {
        while (this.ad * 1529838187 < this.af.length) {
            String[] strArr = this.af;
            int i = this.ad - 90021309;
            this.ad = i;
            String trim = strArr[(i * 1529838187) - 1].trim();
            if (!(trim.startsWith("#") || trim.startsWith("//") || trim.length() == 0)) {
                if (trim.startsWith("[") && trim.endsWith("]")) {
                    this.ad -= -90021309;
                    return null;
                }
                if (-1 != trim.indexOf(61)) {
                    return new String[]{trim.substring(0, trim.indexOf(61)).trim(), trim.substring(trim.indexOf(61) + 1).trim()};
                }
            }
        }
        return null;
    }

    static int ad(kp kpVar, int i, int i2, int i3) {
        if (kpVar != null) {
            try {
                kr krVar = (kr) kpVar.af((long) i);
                if (krVar != null) {
                    i2 = krVar.af;
                }
            } catch (Throwable e) {
                throw ba.ad(e, "ip.ad(" + ')');
            }
        }
        return i2;
    }
}
